const {tokenize}=require('./questionPatternAnalyzer');
function scoreExamStyle(candidate={},knowledge={}){const text=JSON.stringify(candidate||{}); const q=knowledge.questionTrend||knowledge.questionPatterns||{}; const eq=knowledge.equipmentTrend||{}; const dr=knowledge.drawingTrend||{}; const endings=Object.keys(q.style?.endings||{}); const endingHit=endings.filter(e=>text.includes(e)).length/Math.max(1,endings.length); const keywords=Object.entries(q.frequency?.keywords||{}).sort((a,b)=>b[1]-a[1]).slice(0,30).map(([k])=>k); const keywordHit=keywords.filter(k=>text.includes(k)).length/Math.max(1,keywords.length); const equipTerms=(eq.trend||[]).slice(0,20).map(x=>x.name); const equipHit=equipTerms.filter(k=>text.includes(k)).length/Math.max(1,equipTerms.length); const drawingTerms=(dr.trend||[]).slice(0,10).map(x=>x.type); const drawingHit=drawingTerms.filter(k=>text.includes(k)).length/Math.max(1,drawingTerms.length); const structure=/Q03|Q04|Q05|共通|選択|答案用紙|白図/.test(text)?1:0.75; const score=Math.round(Math.min(100,70+endingHit*6+keywordHit*8+equipHit*7+drawingHit*5+structure*4)); return {score,warning:score<95,components:{endingHit,keywordHit,equipHit,drawingHit,structure},candidateKeywords:tokenize(text).slice(0,40)};}
module.exports={scoreExamStyle};
