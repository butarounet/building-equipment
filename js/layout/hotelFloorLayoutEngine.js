(function(root){function validateNoOverlap(rooms=[]){const overlaps=[]; for(let i=0;i<rooms.length;i++)for(let j=i+1;j<rooms.length;j++){const a=rooms[i],b=rooms[j]; if(a.zone==='corridor'||b.zone==='corridor'||a.zone==='core'||b.zone==='core')continue; if(a.x<b.x+b.width&&a.x+a.width>b.x&&a.y<b.y+b.height&&a.y+a.height>b.y)overlaps.push([a.roomId,b.roomId]);} return overlaps;} const api={validateNoOverlap}; if(typeof module!=='undefined')module.exports=api; root.hotelFloorLayoutEngine=api;})(typeof window!=='undefined'?window:globalThis);
